# React Hooks: A Special function given by react which has special features in functional components came after react version 16

useState Hook : It is used to create state variables for the component and a setter function used to update the state of the variable in virtual dom
when ever the state variable changes react rerender the component
